# Per-machine display settings. Chezmoi creates this file once and never
# rewrites it, so edits here survive every later apply.
#
# The default uses each output's preferred mode. Replace it with explicit
# `monitor = NAME, RESOLUTION@HZ, POSITION, SCALE` lines as needed.
monitor = , preferred, auto, 1
